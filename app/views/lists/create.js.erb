// $('#new-list').remove();
// $('#main_center').show();
// $('#collaboration-users').html("<%= j render partial: 'list_users', locals: {list: current_list} %>");
// $('#list').html("<%= j render partial: 'users/list_user', locals: {user: current_user}  %>");
$('#page-content').html("<%= j render @list %>");
  Materialize.toast("<%= flash[:success] %>", 4000);
