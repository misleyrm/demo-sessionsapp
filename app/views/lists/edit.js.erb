$(document).on('turbolinks:load',function(){

		var url = <%= current_user.avatar('thumb') %>;
		// console.log('background'+ $('#ms-upload-avatar').css("background-image"));
		$('.ms-upload-avatar').css('background-image', 'url(url)');
		// console.log('background'+ $('.ms-upload-avatar').css("background-image"));
})
