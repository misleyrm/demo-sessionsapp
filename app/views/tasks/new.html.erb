<div class="forms">
	<h2 class="center"><%= (params[:type].present? || params[:type]=="blocker") ? 'New Blocker' : 'New Task'  %></h2>
	<%= (params[:type].present? || params[:type]=="blocker") ? (render 'formBlocker') : (render 'form')  %>
</div>

<!-- <div class="links">
	<%= link_to 'Back', lists_path, remote: true, data: {behavior: 'modal-form'} %>
</div> -->
